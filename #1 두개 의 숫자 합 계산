/* 두개의 숫자를 합해서 
계산해주는 프로그램*/

#include <stdio.h>
int main (void)
{
int x;
int y; 
int sum; //변수선언

x=100;
y=100; //변수할당

sum = x+y; // 연산 할당

printf("두수의합은: %d", sum); //형식지정자

return0;

}

//전처리기: 라이브러리함수들의 정의가 들어있는 헤더파일을 #으로 임포트헤서 커맢일러가 읽을수있게 실제로 stdio.h에 printf함수 정의가 들어있어 stdio.h 임포트해야만 사용가능 
